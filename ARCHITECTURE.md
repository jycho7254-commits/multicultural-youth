# ARCHITECTURE — 어울림 마음쉼터 (v7, 최신)

> v7: 게시판 Supabase 연동 계층, 비밀글 타인 마스킹, 유튜브뮤직 플레이리스트,
> 대표 정보 갱신(박소영/010-6410-0146), 척도 CSS 근본수정(v1 잔여 3열 제거)

## 시스템 개요

```
┌────────────────────────────────────────────────────────┐
│  Phase 1 (라이브) — 정적 사이트 (GitHub Pages)           │
│                                                        │
│  index.html (단일 파일, 의존성 0, 7탭 SPA 구조)          │
│   ├─ 뷰 라우터: showView() — 섹션 단일 표시 + 해시       │
│   │   └─ 히어로/카드는 쉼터(홈) 탭 전용                  │
│   ├─ 마음체크: PHQ-9/GAD-7/WHO-5 (클라이언트 채점)      │
│   │   ├─ 척도 4열/5열 자동 분기 (minmax(0,1fr))         │
│   │   └─ 문항9 ≥1 → 1393 크라이시스 (다국어)            │
│   ├─ 게시판: 비밀글(owner_key 대조) + 따뜻해요           │
│   ├─ 함께듣기: YT뮤직 링크 5종 + 신청곡                  │
│   ├─ 프로그램 8종 모달 (상세 + YT 검색)                  │
│   └─ i18n: I18N 99키×3 + DYN 31키 (동적 텍스트)         │
└────────────────────────────────────────────────────────┘
              │
              ▼ (BOARD_DB_URL/KEY 입력 시)
┌────────────────────────────────────────────────────────┐
│  Supabase (게시판 공유 저장소)                           │
│   ├─ eoullim_board (RLS: 공개 읽기/쓰기)                │
│   │   └─ view eoullim_board_public — 비밀글 text=null   │
│   ├─ eoullim_songs (신청곡)                             │
│   └─ 실패 시 localStorage 폴백 (자동)                    │
└────────────────────────────────────────────────────────┘
```

## 핵심 컴포넌트

### 1. 뷰 라우터 (탭 전환)
- `showView(sec)` — 7섹션 단일 표시, 히어로/카드는 shelter 전용
- 세션 유지(sessionStorage) + URL 해시 공유 + rAF 스크롤 최상단
- 모바일: nav 2행(로고/언어 + 탭 4열×2행 그리드), 스크롤 시 헤더 축소

### 2. 심리검사 엔진
| 도구 | 문항 | 채점 | 위기 연계 |
|---|---|---|---|
| PHQ-9 | 9 | 0~27 (5/10/15 컷) | 문항9 ≥1 → 1393 박스 |
| GAD-7 | 7 | 0~21 (5/10 컷) | - |
| WHO-5 | 5 | 100점 환산 (29/50 컷) | - |
- 옵션 라벨: `optLabel()` — DYN 사전 번경, 척도 열수 자동 분기

### 3. 게시판 (이중 저장소)
```
쓰기: DB_ON ? dbInsert(Supabase) : localStorage
읽기: loadBoard() → 원격 캐시 → 없으면 localStorage
비밀글: owner_key === localStorage('eoullim_mykey') 만 내용 표시
        타인 → "🔒 비밀글이에요 — 작성자만 볼 수 있어요"
```

### 4. i18n (전체 다국어)
- `I18N` 99키 (정적) + `DYN` 31키 (alert/placeholder/검사옵션/크라이시스)
- `dataset.orig` 원문 백업 → KO 복귀 / 번역 누락 폴백
- 변수 충돌 방지: submit 핸들러 `T`, 라벨 `optLabel()`

## CSS 아키텍처 (웜톤)

```
--sage:#d98e6b (테라코타) --sage-dark:#b96f4e
--cream:#fdf8f0 --cream2:#f9efe2
```
- 모바일 우선 미디어쿼리 (820px)
- 척도: `.opts` 4열 / `.test5 .opts` 5열 — `minmax(0,1fr)!important`

## Phase 로드맵

| Phase | 내용 | 상태 |
|---|---|---|
| 1 | 정적 사이트 전 기능 | ✅ 라이브 |
| 2 | Supabase 활성화 (키 입력만) | 코드 완료, 대기 |
| 3 | 12주 집단 아르테라피 + 성과 리포트 | 데이터 축적 후 |
| 4 | 개입 모델 표준화·타 지역 이전 | 2028~ |

## 보안·윤리

- [x] 검사 비저장 / 비식별 / "선별 도구" 고지
- [x] 위기 연락처 상시 노출 (1393/1388/112/1577-0199)
- [x] 비밀글 서버 필터링 (view에서 text=null) + 프론트 마스킹 이중 방어
- [ ] Phase 2: 14세 동의 절차, 데이터 처리방침 페이지

## 배포

```
git push origin main → GitHub Pages
https://jycho7254-commits.github.io/multicultural-youth/
```

## OMP 검증 이력

1~3차(기능/라이브/재검증) → v3(모바일+PC 22/22) → v4(다국어 잔존 0) → v5~7(7탭 가시·척도 1줄, iPhone SE 375px 실측)
